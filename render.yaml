services:
  - type: web
    name: jo-tikets
    env: python
    buildCommand: "pip install -r requirements.txt && python manage.py collectstatic --noinput && python manage.py migrate"
    startCommand: "gunicorn jo_tikets.wsgi:application"
    pythonVersion: 3.12.4
